[package]
name = "qatool"
version = "0.1.0"
edition = "2021"
description = "CLI tool for CL8Y ecosystem QA"

[dependencies]
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
toml = "0.8"
serde = { version = "1", features = ["derive"] }
